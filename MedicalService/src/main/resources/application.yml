spring:
  application:
    name: medical-service

  datasource:
    url: jdbc:mysql://localhost:3306/servicios_medicos
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka